/**
 * Shared package for Aska Monorepo
 * Contains common utilities, types, and functions used across apps
 */

// Database types (placeholder, replace when Supabase types are generated)
export type Database = Record<string, never>;

// Re-export commonly used utilities
export * from "./types";
export * from "./utils";
